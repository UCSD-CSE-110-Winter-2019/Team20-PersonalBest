package cse110.ucsd.team20_personalbest;

import android.graphics.Color;
import android.graphics.drawable.ColorDrawable;
import android.graphics.drawable.Drawable;
import android.support.test.rule.ActivityTestRule;
import android.widget.Button;
import android.widget.TextView;

import org.junit.Before;
import org.junit.Rule;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class    StartStopUnitTest {

    private Button startStopbutton;
    private TextView textSteps;
    //private MainActivity activity;
/*
    @Rule
    public ActivityTestRule<MainActivity> activity = new ActivityTestRule<MainActivity>(MainActivity.class);
    //public ActivityTestRule<MainActivity> activity = new ActivityTestRule<MainActivity>(MainActivity.class);

    @Before
    public void setup() {
        startStopbutton = (Button) activity.getActivity().findViewById(R.id.startStop);
    }

    // test whether or not the button initializes as start
    @Test
    public void test_initialstart() {
        //assertEquals( ,startStopbutton);
    }

    // test that the button changes on cicks
    @Test
    public void test_buttonchanges() {
        ColorDrawable buttonColor1 = (ColorDrawable) startStopbutton.getBackground();
        assertEquals(Color.GREEN, buttonColor1.getColor());
        startStopbutton.performClick();
        ColorDrawable buttonColor2 = (ColorDrawable) startStopbutton.getBackground();
        assertEquals(Color.RED, buttonColor2.getColor());
        startStopbutton.performClick();
        ColorDrawable buttonColor3 = (ColorDrawable) startStopbutton.getBackground();
        assertEquals(Color.GREEN, buttonColor3.getColor());
    }

    // test that the toast is sent on button presses
    @Test
    public void test_toastSent() {

    }
*/
} // end class
